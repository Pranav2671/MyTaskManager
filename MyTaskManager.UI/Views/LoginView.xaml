<UserControl x:Class="MyTaskManager.UI.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="300" Width="400">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Username:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtUsername" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Password:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Button Content="Login" Grid.Row="2" Grid.Column="1" Width="100" HorizontalAlignment="Right" Margin="5"
                Click="Login_Click"/>

        <TextBlock x:Name="txtMessage" Grid.Row="3" Grid.ColumnSpan="2" Foreground="Red" Margin="5"/>

        <Button Content="Register" Grid.Row="4" Grid.Column="1" Width="100" HorizontalAlignment="Right" Margin="5"
                Click="Register_Click"/>
    </Grid>
</UserControl>
