<UserControl x:Class="MyTaskManager.UI.Views.AddTaskView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <StackPanel Margin="10" Width="300">
        <TextBlock Text="Add New Task" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <TextBlock Text="Title:"/>
        <TextBox x:Name="TitleTextBox" Margin="0,0,0,10"/>

        <TextBlock Text="Status:"/>
        <ComboBox x:Name="StatusComboBox" Margin="0,0,0,10">
            <ComboBoxItem Content="Pending" Tag="0" IsSelected="True"/>
            <ComboBoxItem Content="InProgress" Tag="1"/>
            <ComboBoxItem Content="Completed" Tag="2"/>
            <ComboBoxItem Content="Dropped" Tag="3"/>
        </ComboBox>

        <TextBlock Text="Due Date (optional):"/>
        <DatePicker x:Name="DueDatePicker" Margin="0,0,0,10"/>

        <Button Content="Add Task" Click="AddTask_Click" Width="100"/>
    </StackPanel>
</UserControl>
